# Capture SOP — all_telop 数据采集操作规范

本文说明使用 `utils/all_telop` 进行多模态采集时的按键语义与统计约定。

## 按键

| 键 | 作用 |
| --- | --- |
| `p` | 开始 / 继续：相机写盘、夹爪跟随、机械臂遥操（rezero） |
| `q` | 暂停：停止相机写盘与夹爪跟随，机械臂保持姿态；**同一 episode 可再按 `p` 续录** |
| `Ctrl+C` | 结束进程并落盘、打印统计 |

## 暂停与续录（q → p）

- 按 `q` 后进入暂停，**不写入**彩色 / 深度 / 鱼眼 / 夹爪 CSV 与图像。
- 再按 `p` 从暂停恢复，数据追加到**同一** episode 目录。
- **要求：从按 `q` 到再按 `p` 的墙钟间隔必须大于 200 ms（0.2 s）。**

### 为什么要大于 200 ms

退出时帧率统计会：

1. 用键盘累计的 **session 激活时长**（各段 `p`→`q` 之和）计算 **总耗时** 与 **average rate**；
2. 对相邻帧时间戳间隔做过滤：间隔 **> 0.2 s** 视为暂停间隙，**不参与** min / max / stddev。

若 `q`→`p` 短于 200 ms，暂停空隙可能被当成正常帧间隔，污染间隔统计。请保证暂停至少 **200 ms** 以上再按 `p`。

代码常量与本 SOP 一致：`utils/all_telop/src_all/statistics.cpp` 中 `kMaxInSessionDt = 0.2`。

## 统计含义（Ctrl+C 后）

- **average rate / 总耗时**：仅累计 session 激活时间，**不含** `q`→`p` 暂停。
- **min / max / stddev / window**：仅使用 ≤ 0.2 s 的帧间隔；暂停造成的大间隙已排除。

## 简要流程

1. 终端 1：启动 `pika_locator`。
2. CAN、串口、相机就绪后，启动 `all_telop`。
3. Ready 后按 `p` 开始采集；需要中断动作时按 `q`（停够 **>200 ms**），再按 `p` 续录。
4. 采集结束按 `Ctrl+C`，查看打印的帧率统计与 `~/pika_ros/data/{episode}/` 数据。
